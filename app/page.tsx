import MissionBoard from "@/components/MissionBoard";
import {missions,frontier} from "@/data/missions";
import {longReads,updates} from "@/data/updates";
export default function Home(){return <main><header className="topbar"><div><div className="kicker">Humanity&apos;s active eyes & machines beyond Earth</div><h1>SPACE <span>//</span> INTELLIGENCE</h1></div><div className="live">● PUBLIC V1</div></header><section className="ticker"><span><b>MISSION CONTROL</b> active mission terminal</span><span><b>FRONTIER</b> deep-space distance context</span><span><b>AI LAYER</b> significance, not press-release volume</span><span><b>SOURCES</b> NASA / JPL / ESA first</span></section><section className="grid"><div className="card metric"><div className="metricLabel">Featured systems</div><div className="metricValue">{missions.length}</div></div><div className="card metric"><div className="metricLabel">Interstellar probes</div><div className="metricValue">2</div></div><div className="card metric"><div className="metricLabel">Mars surface robots</div><div className="metricValue">2</div></div><div className="card metric"><div className="metricLabel">Jupiter-bound craft</div><div className="metricValue">2</div></div><div className="card frontier"><div className="sectionTitle"><h2>At the frontier</h2><div>Approx. heliocentric distance</div></div>{frontier.map(i=><div className="bar" key={i.name}><span>{i.name}</span><div className="track"><div className="fill" style={{width:`${i.pct}%`}}/></div><span className="barValue">{i.value}</span></div>)}<p className="fineprint">Rounded for human orientation, not navigation. Live ephemerides are a V1.1 source-adapter task.</p></div><div className="card updates"><div className="sectionTitle"><h2>What matters now</h2><div>AI-ranked</div></div>{updates.map(u=><article className="update" key={u.title}><div className="updateTime">{u.time}</div><div className="updateHead">{u.title}</div><div className="updateBody">{u.body}</div></article>)}</div></section><section><div className="sectionHeading"><div><div className="kicker">Active systems</div><h2>Mission board</h2></div><div className="sub">Source links open official mission feeds</div></div><MissionBoard missions={missions}/></section><section><div className="sectionHeading"><div><div className="kicker">Editorial layer</div><h2>Long reads</h2></div><div className="sub">Selected for explanatory value</div></div><div className="reading">{longReads.map(i=><a className="read" href={i.href} target="_blank" rel="noreferrer" key={i.href}><div className="pub">{i.publisher}</div><div className="readTitle">{i.title}</div><div className="readDesc">{i.description}</div></a>)}</div></section><footer><b>Source philosophy.</b> Mission facts should resolve to primary sources. AI summary and interpretation are separate layers. This deployment ships with a curated snapshot so the public site works before credentials or a database are configured.</footer></main>}
